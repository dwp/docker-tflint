groups:
  - name: master
    jobs:
      - management-dev
      - management
  - name: pull-request
    jobs:
      - docker-tflint-pr
  - name: mirror
    jobs:
      - mirror-dwpdigital-docker-tflint-dev
      - mirror-dwpdigital-docker-tflint
